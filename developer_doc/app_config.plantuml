@startuml
title 动态修改服务器配置功能（精确版）

actor 用户
participant "SettingsViewModel" as VM
participant "AppPreferences" as Prefs
participant "DataStore" as DS

用户 -> VM: 调用 updateApiBaseUrl(newUrl: String)
VM -> Prefs: setApiBaseUrl(dataStore: DataStore, url: String)
Prefs -> DS: edit { ... }
DS --> Prefs: 保存完成
Prefs --> VM: 无返回值
用户 -> VM: 读取配置（重新进入界面）
VM -> Prefs: getApiBaseUrlFlow(dataStore: DataStore)
Prefs -> DS: dataStore.data
DS --> Prefs: 发射 Preferences
Prefs --> VM: Flow<String> 发射 URL
VM --> 用户: 显示最新 URL
@enduml