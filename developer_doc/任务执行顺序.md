# 任务执行顺序

> 本文档定义了完成 `全部核心任务.md` 中各任务的推荐顺序，并跟踪每个任务的当前状态。  
> 状态说明：✅ 已完成 &nbsp;&nbsp; 🟡 进行中 &nbsp;&nbsp; ⭕ 待开始  
> **阶段标题** 设有锚点，可从 `全部核心任务.md` 中的任务 ID 点击跳转至此。

## 任务执行顺序表

| 阶段 | 任务 ID | 任务描述 | 依赖条件 | 预期产出 | 状态 |
|------|---------|----------|----------|----------|------|
| <a id="phase1"></a>**阶段一：基础链路验证** | T-007 | 在 `MainActivity` 添加跳转按钮 | 无 | 点击按钮能打开 `WebViewActivity` | ✅ 已完成 |
| | T-008 | 准备测试 HTML 页面（本地 assets 或远端） | 无 | 页面包含调用 JS Bridge 的按钮 | ✅ 已完成 |
| | 运行测试 | 验证 T-004~T-006（WebView 三文件） | T-007, T-008 | 确认 WebView 能加载页面，Toast 弹出 | ✅ 已完成 |
| <a id="phase2"></a>**阶段二：传感器模拟采集与存储** | T-013 | 添加 AndroidManifest 权限（`BODY_SENSORS`, `INTERNET`） | 无 | 应用能请求传感器权限 | ⭕ 待开始 |
| | T-009, T-010 | 完善 `HeartRateCollector` 和 `AccelerometerCollector` 的模拟数据生成 | T-013 | 每秒生成随机心率/加速度值 | ⭕ 待开始 |
| | T-011 | 确认 `SensorDataRepository` 能存储到 Room | T-025~T-034 | 数据插入成功，可通过 ADB 查看 | ✅ 已完成 |
| <a id="phase3"></a>**阶段三：WorkManager 上传（本地 mock）** | T-012 | 实现 `SensorUploadWorker` 的网络上传逻辑（先 mock 后端） | T-011 | 定时上传任务能触发并标记 `synced` | ⭕ 待开始 |
| | T-014 | 在 `WebViewBridge` 中实现 `startAutoUpload` / `stopAutoUpload`，调用 WorkManager | T-012 | 网页按钮能启停上传任务 | ⭕ 待开始 |
| <a id="phase4"></a>**阶段四：真实后端集成** | T-036 | 实现 `NetlifyAuthenticator`（添加 API Key） | 无 | 请求携带认证头 | ⭕ 待开始 |
| | T-037 | 配置 Retrofit 单例（在 Application 或 DI 中） | T-035 | 全局可用 `NetlifyApi` 实例 | ⭕ 待开始 |
| | T-039, T-040 | 完善 `SyncRepository` 和 `SyncWorker` 的网络调用 | T-037 | 能同步消费数据到 Netlify DB | ⭕ 待开始 |
| | T-041 | 初始化定期同步任务（Application 中启动） | T-040 | 后台定期同步 | ⭕ 待开始 |
| <a id="phase5"></a>**阶段五：工具类与测试** | T-042~T-044 | 完善 `FileUtils`, `NetworkUtils`, `PermissionHelper` | 无 | 提供常用辅助功能 | ⭕ 待开始 |
| | T-048~T-050 | 编写单元测试、仪表化测试、手动集成测试 | 阶段四完成后 | 保障代码质量 | ⭕ 待开始 |

> **当前状态**：阶段一已完成，下一步进入阶段二，开始 T-013（添加权限）和 T-009/T-010（完善传感器采集）。

## 如何向 AI 提问以生成此格式的文档？

（此部分保持不变，略），