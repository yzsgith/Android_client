# 辅助功能映射表 - WebView 交互

> 本文档列出为核心功能 `webview_interaction` 提供增强的辅助组件。这些组件不修改核心代码，可独立启停。

## 辅助组件列表

| 辅助组件 | 作用 | 使用方式 | 相关文件 | 时序图 | 依赖配置 |
|----------|------|----------|----------|--------|----------|
| 浏览器工具栏 | 提供后退、前进、地址栏、退出按钮，支持页面状态同步 | **手动集成**：创建 `BrowserToolbarHelper` 实例并在 `WebViewClient` 中调用其方法。<br>**推荐**：调用 `enableToolbar()` 扩展函数。 | - 辅助类：[`BrowserToolbarHelper.kt`](../../../app/src/main/java/.../BrowserToolbarHelper.kt)<br>- 扩展函数：[`ToolbarExtensions.kt`](../../../app/src/main/java/.../ToolbarExtensions.kt) | [`toolbar_design.plantuml`](./toolbar_design.plantuml) | 布局文件需包含指定 ID 控件 |

## 使用“浏览器工具栏”示例

### 方式一：手动集成

```kotlin
class WebViewActivity : AppCompatActivity() {
    private lateinit var toolbar: BrowserToolbarHelper

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_webview)
        val webView = findViewById<WebView>(R.id.webView)
        toolbar = BrowserToolbarHelper(this, webView)
        webView.webViewClient = object : WebViewClient() {
            override fun onPageStarted(view: WebView?, url: String?, favicon: Bitmap?) {
                toolbar.onPageStarted(url)
            }
            override fun onPageFinished(view: WebView?, url: String?) {
                toolbar.onPageFinished()
            }
        }
    }
}
```
### 方式二：推荐方式（扩展函数）
```kotlin

复制

下载
class WebViewActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_webview)
        // ... 确保 WebView 已初始化
        enableToolbar()   // 一行启用
    }
}
```
```text
使用说明
推荐使用扩展函数 enableToolbar()，最简洁且内部自动处理生命周期绑定。

手动方式适合需要自定义工具栏行为（如增加额外控件或修改点击逻辑）的场景。

工具栏图标资源（ic_back, ic_forward）需自行添加到 res/drawable 目录。

相关文档
主功能映射表

核心时序图

总览.md
}
```