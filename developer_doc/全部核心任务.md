# 全部核心任务

> 本文档基于 Android 客户端架构设计，列出所有待完成的核心任务。每个任务均对应一个新建文件或修改点，且需在文件中添加 `// TODO` 注释。  
> **保存位置**：`developer_doc/全部核心任务.md`

## 一、项目基础配置

- [x] 创建 Compose 空项目，包名 `com.sea.auspicious_sign`
- [x] 配置 `build.gradle.kts`（已包含 Compose 基础依赖）
- [ ] **补充 Gradle 依赖**（手动添加到 `app/build.gradle.kts`）  
  需添加：Room、WorkManager、Retrofit、OkHttp、Accompanist 权限等（参考之前给出的依赖代码块）

## 二、WebView 模块（用户主动交互）

### 新建文件

- [ ] `webview/WebViewActivity.kt`  
  **作用**：加载服务器前端页面，配置安全策略  
  **TODO**：实现 `onCreate` 中初始化 WebView，设置域名白名单，启用 JS

- [ ] `webview/WebViewBridge.kt`  
  **作用**：暴露原生方法给 JS（启停自动采集、获取数据、选择文件）  
  **TODO**：使用 `@JavascriptInterface`，实现方法体

- [ ] `webview/WebViewSecurity.kt`  
  **作用**：集中管理 WebView 安全配置  
  **TODO**：提供扩展函数 `WebView.applySecuritySettings()`

### 集成与测试

- [ ] 在 `MainActivity.kt` 添加跳转 `WebViewActivity` 的按钮
- [ ] 准备测试 HTML 页面，验证 JS Bridge 通信

## 三、传感器自动采集模块（后台模式）

### 新建文件

- [ ] `sensor/collector/HeartRateCollector.kt`  
  **作用**：监听心率传感器（无硬件时模拟）  
  **TODO**：实现 `SensorEventListener`，注册/注销，数据回调存储

- [ ] `sensor/collector/AccelerometerCollector.kt`  
  **作用**：监听加速度计  
  **TODO**：实现采集逻辑，配合步数处理器

- [ ] `sensor/SensorDataRepository.kt`  
  **作用**：封装 Room 操作，提供 `insertRawData()`、`getUnsyncedData()`  
  **TODO**：注入 DAO，实现数据存储

- [ ] `sensor/upload/SensorUploadWorker.kt`  
  **作用**：`Worker` 周期性上传未同步的传感器数据  
  **TODO**：调用 `NetlifyApi.uploadSensorData()`，更新同步状态

### 权限与启动

- [ ] 在 `AndroidManifest.xml` 添加 `BODY_SENSORS`、`INTERNET` 权限
- [ ] 在 `WebViewBridge` 中实现 `startAutoUpload(intervalMs: Int)` 和 `stopAutoUpload()`
- [ ] 使用 `WorkManager` 启动/停止周期性任务

## 四、数据处理流水线（抽象三阶段）

### 核心抽象

- [ ] `domain/model/DataStages.kt`  
  **作用**：定义 `RawData`、`TransformedData`、`ConsumableData` 标记接口

- [ ] `domain/processor/DataProcessor.kt`  
  **作用**：定义 `suspend fun process(input: IN): OUT` 接口

- [ ] `domain/processor/pipeline/FlexiblePipeline.kt`  
  **作用**：组合多个 `DataProcessor`，支持链式调用  
  **TODO**：实现 `execute(start: Any): Any`，处理类型擦除

### 具体处理器示例

- [ ] `domain/processor/impl/HeartRateSmoother.kt`  
  **作用**：`RawHeartRate` → `SmoothedHeartRate`（平滑滤波）

- [ ] `domain/processor/impl/HeartRateAnalyzer.kt`  
  **作用**：`SmoothedHeartRate` → `HeartRateAlert`（阈值告警）

- [ ] `domain/processor/impl/StepCounterProcessor.kt`  
  **作用**：`RawAccelerometer` → `StepCount`（步数检测）

### 流水线注册与执行

- [ ] `domain/repository/DataProcessRepository.kt`  
  **作用**：协调采集、处理、存储全流程  
  **TODO**：提供 `processRawData(raw: RawData)` 方法，根据类型调用流水线，存入各阶段表，触发同步

## 五、数据存储层（Room）

### 实体（Entity）

- [ ] `data/local/entity/RawDataEntity.kt`  
  字段：`id`, `type`, `timestamp`, `dataJson`, `blobKey`（实现 `RawData`）

- [ ] `data/local/entity/TransformedDataEntity.kt`  
  字段：`id`, `rawDataId`, `type`, `resultJson`, `createdAt`（实现 `TransformedData`）

- [ ] `data/local/entity/ConsumableDataEntity.kt`  
  字段：`id`, `transformId`, `type`, `resultJson`, `synced`（实现 `ConsumableData`）

- [ ] `data/local/entity/SyncQueueEntity.kt`  
  字段：`id`, `consumableDataId`, `syncStatus`, `retryCount`

### DAO 接口

- [ ] `data/local/dao/RawDataDao.kt`
- [ ] `data/local/dao/TransformedDataDao.kt`
- [ ] `data/local/dao/ConsumableDataDao.kt`
- [ ] `data/local/dao/SyncQueueDao.kt`

### 数据库抽象类

- [ ] `data/local/AppDatabase.kt`  
  **作用**：`@Database` 注解，提供各 DAO 实例，定义迁移策略

## 六、网络通信（与 Astro 后端交互）

- [ ] `data/remote/NetlifyApi.kt`  
  **作用**：Retrofit 接口，定义端点：
    - `POST /api/upload`（上传文件到 Blobs）
    - `POST /api/process`（请求云端处理）
    - `GET /api/result/{jobId}`（获取结果）
    - `POST /api/mappings`（同步本地映射）
    - `POST /api/sensor-data`（批量上传传感器数据）

- [ ] `data/remote/NetlifyAuthenticator.kt`  
  **作用**：OkHttp 拦截器，添加 API Key 或 JWT

- [ ] 配置 Retrofit 单例（在 `Application` 或依赖注入中）

## 七、同步管理（WorkManager + 本地队列）

- [ ] `sync/SyncStatus.kt`  
  **作用**：枚举 `PENDING`, `SUCCESS`, `FAILED`

- [ ] `sync/SyncRepository.kt`  
  **作用**：`enqueueSync()`、`processPendingSync()`（调用 API，更新状态，指数退避）

- [ ] `sync/SyncWorker.kt`  
  **作用**：`CoroutineWorker` 周期性执行 `processPendingSync()`，设置网络约束

- [ ] 初始化定期同步任务（在 `Application` 或 `DataProcessRepository` 中）

## 八、工具类与辅助功能

- [ ] `utils/FileUtils.kt`（图片压缩、Base64、URI 转流）
- [ ] `utils/NetworkUtils.kt`（网络类型检查）
- [ ] `utils/PermissionHelper.kt`（请求 `BODY_SENSORS` 权限，使用 Accompanist 或 `ActivityResultLauncher`）

## 九、文档与架构图（PlantUML + Markdown）

所有 `.md` 文件放在 `developer_doc/` 下：

- [x] `总览.md`（已生成，包含文件索引）
- [ ] `架构设计.md`（PlantUML 组件图、时序图、数据流图）
- [ ] `API设计.md`（Astro 后端端点详细说明）
- [ ] `数据库设计.md`（Room 表结构、字段说明、索引）
- [ ] `开发步骤.md`（按优先级排序的实施计划）

## 十、测试与验证

- [ ] 单元测试：各 `DataProcessor` 的逻辑（`src/test/java`）
- [ ] 仪表化测试：WebView JS Bridge、传感器采集、Room 操作（`src/androidTest/java`）
- [ ] 手动集成测试：完整流程（采集 → 处理 → 同步 → 查询）

---

## 优先级建议

1. **基础链路**：WebView 加载 + JS Bridge 简单调用
2. **传感器采集 + Room 存储**（使用模拟数据）
3. **数据处理流水线**（先做简单处理器）
4. **WorkManager 同步**（mock 后端）
5. **完善所有处理器、错误处理、UI 反馈和安全配置**

---

**生成时间**：2026-04-13  
**维护者**：项目团队