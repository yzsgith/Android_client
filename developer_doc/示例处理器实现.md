# 示例处理器实现

> 本文档列出数据处理流水线中的示例处理器实现（参考），用于演示 `DataProcessor` 接口的用法。  
> 所有文件均已迁移至 `:domain` 纯 Kotlin 模块，不依赖 Android SDK。

## 一、相关文件列表

| 作用 | 文件（点击链接跳转） |
| --- | --- |
| 心率平滑处理器：原始心率 → 平滑心率 | [domain/src/main/java/com/sea/auspicious_sign/domain/processor/impl/HeartRateSmoother.kt](../domain/src/main/java/com/sea/auspicious_sign/domain/processor/impl/HeartRateSmoother.kt) |
| 心率分析处理器：平滑心率 → 告警等级 | [domain/src/main/java/com/sea/auspicious_sign/domain/processor/impl/HeartRateAnalyzer.kt](../domain/src/main/java/com/sea/auspicious_sign/domain/processor/impl/HeartRateAnalyzer.kt) |

## 二、文件内容

### 1. HeartRateSmoother.kt

```kotlin
package com.sea.auspicious_sign.domain.processor.impl

import com.sea.auspicious_sign.domain.model.RawHeartRate
import com.sea.auspicious_sign.domain.model.SmoothedHeartRate
import com.sea.auspicious_sign.domain.processor.DataProcessor

/**
 * 心率平滑处理器：原始心率 -> 平滑心率
 * TODO: 实现滑动平均或中值滤波算法
 */
class HeartRateSmoother : DataProcessor<RawHeartRate, SmoothedHeartRate> {
    override suspend fun process(input: RawHeartRate): SmoothedHeartRate {
        // TODO: 实现平滑算法
        return SmoothedHeartRate(
            bpm = input.bpm,
            isValid = input.confidence > 0.7f,
            timestamp = input.timestamp
        )
    }
}